(function(){const nav=document.getElementById('vp-main-nav');const toggle=document.querySelector('.vp-menu-toggle');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open));});}document.querySelectorAll('.vp-nav-toggle').forEach(btn=>{btn.addEventListener('click',e=>{e.stopPropagation();const group=btn.closest('.vp-nav-group');const open=group.classList.toggle('is-open');btn.setAttribute('aria-expanded',String(open));document.querySelectorAll('.vp-nav-group.is-open').forEach(other=>{if(other!==group){other.classList.remove('is-open');const b=other.querySelector('.vp-nav-toggle');if(b)b.setAttribute('aria-expanded','false');}});});});document.addEventListener('click',e=>{document.querySelectorAll('.vp-nav-group.is-open').forEach(group=>{if(!group.contains(e.target)){group.classList.remove('is-open');const b=group.querySelector('.vp-nav-toggle');if(b)b.setAttribute('aria-expanded','false');}});});const s='.info-card,.authority-card,.process-card,.testimonial-card,.offer-card,.benefit-card,.network-card,.step-card,.faq-card,.stage-card,.soft-card,.value-card,.promise-card,.proof-card,.about-card,.reseau-card,.local-card';document.querySelectorAll(s).forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--x',(e.clientX-r.left)+'px');card.style.setProperty('--y',(e.clientY-r.top)+'px');});});})();